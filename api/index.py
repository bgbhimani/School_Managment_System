from app.main import app

# Vercel's Python runtime will look for an ASGI application object named `app`.
# This file exposes the FastAPI `app` defined in `app/main.py` so Vercel can
# serve the application as a serverless function.
